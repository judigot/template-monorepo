# Base URL of the deployed Hono API used by Server Components (server-only).
# Falls back to http://localhost:3000 during local development.
API_URL=

# Only needed if a Client Component ever fetches the API directly from the
# browser. Unused by the current pages; safe to leave unset.
NEXT_PUBLIC_API_URL=
