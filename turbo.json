{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": ["**/.env.*local", "bunfig.toml"],
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": ["VITE_API_URL", "API_URL", "NEXT_PUBLIC_API_URL", "CORS_ORIGINS"]
    },
    "@judigot/api#build": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "env": ["CORS_ORIGINS"]
    },
    "typecheck": {},
    "lint": {},
    "clean": {
      "cache": false
    }
  }
}
